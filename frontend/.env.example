# Base URL of the FastAPI backend (src/api/main.py).
# Copy this file to `.env` to override the default (http://localhost:8000).
# When deployed same-origin on HF Spaces (Phase 8), set this to "" so requests
# go to the same host that serves the frontend.
VITE_API_BASE_URL=http://localhost:8000
